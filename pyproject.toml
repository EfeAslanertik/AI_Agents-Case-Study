[project]
name = "AI_Agents"
version = "0.1.0"
description = "Autonomous agents for dataset validation and audit logging."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy"
]